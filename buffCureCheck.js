const version = '0.0.2';
const show = true;
const verbose = true;

	if (typeof action !== 'undefined') {
		if (action.tag === 'cure') {
			//  buff needs to be removed
			await item.delete;
		}
	}
return